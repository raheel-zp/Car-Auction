import { createContext } from "react";

// export const Adminloggedin = createContext({});
export const userloggedIn = createContext(null);
export const userData = createContext({});
// export const cartNotification = createContext({});